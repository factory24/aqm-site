apiVersion: v1
kind: Service                                 # Type of the kubernetes resource
metadata:
  name: hydrate-dashboard                # Name of the Kubernetes resource
  namespace: athari
  labels:                                     # Labels that will be applied to the resource
    app: hydrate-dashboard
spec:
#  type: NodePort                              # Gives each node an external IP that’s accessible from outside the cluster and also opens a Port.
  selector:
    app: hydrate-dashboard               # Selects any Pod with labels `app=fullstack-app-postgres`
  ports:
    - name: http
      port: 80
      protocol: TCP
